import { test, expect } from '@playwright/test';

test.describe('Data Loading', () => {
  test('data appears in tables within timeout', async ({ page }) => {
    // TODO: Implement data loading tests
    test.skip();
  });
});
